---
# Per-host toggle — set to false in a host's inventory/host_vars/<hostname>.yml to skip this
# role entirely (see playbooks/site.yml). Requires k3s_enabled: true (see
# roles/k3s/defaults/main.yml) — Flux bootstrap assumes a running K3s cluster.
flux_enabled: true
