---
# Per-host toggle — set to false in a host's inventory/host_vars/<hostname>.yml to skip this
# role entirely (see playbooks/site.yml). Note: the flux role depends on k3s — disabling k3s
# while flux is enabled fails the dependency guards in both playbooks/site.yml and
# roles/flux/tasks/preflight.yml.
k3s_enabled: true
