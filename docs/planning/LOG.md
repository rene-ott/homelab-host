# Log

One line per shipped change, newest first — the scannable *why*. Git holds the diff
(`git log --grep=<keyword>`). Older entries describe the repo state at that time and may mention removed setup surfaces.

- 2026-07-04 — documented Windows 11 SMB fix in README: `AllowInsecureGuestAuth` registry command (Windows blocks insecure guest logons by default, needed to reach the guest-only `samba` share) plus an optional combined command that also maps a DNS name to the server IP in the Windows hosts file

- 2026-07-04 — added `samba` role: guest, read-write SMB share on `/srv/media` (force user/group 1000:1000 matching storage's ownership), port 445 opened in firewall, `nmbd` explicitly disabled (SMB2+ direct only) — supersedes the placeholder samba role removed 2026-06-20 for being speculative; this one is a full implementation

- 2026-07-04 — added `storage` role: ensures shared, app-agnostic directory roots (`/srv/media{,/movies,/tv,/music}`, `/srv/config`, `/srv/cache`) exist, owned 1000:1000, mode 0755, ahead of k3s in site.yml — apps still self-provision their own subdirectories via hostPath in homelab-cluster

- 2026-07-04 — added `scripts/cluster.sh`: interactive bootstrap/teardown/verify menu for the K3s+Flux cluster, driving `site.yml` and `teardown-k3s.yml` with the relevant tags — replaces the single-purpose `teardown-k3s.sh` from the same day

- 2026-07-04 — added `scripts/quick-deploy.sh`: chains `bootstrap-user.yml` → `site.yml` → `verify.yml` in one prompt-and-go run for a fresh server

- 2026-07-04 — added `playbooks/teardown-k3s.yml`: uninstalls K3s only (service, data, certs, CNI/iptables, binaries) via the installer's own uninstall script, guarded by `-e teardown_confirm=true` since it's destructive — lets `site.yml` re-provision K3s + Flux cleanly afterward

- 2026-07-04 — collapsed flux_bootstrap's SOPS age key check from stat+assert into a single assert (`is file`), matching the pattern already used in flux_auth's deploy-key check

- 2026-07-04 — audited all roles/playbooks for unnecessary tasks and ordering; site.yml role order confirmed correct, no removals needed; documented the intentional duplicate flux-system namespace check in flux_auth/flux_bootstrap (kept for `--tags` standalone runs, not a bug)

- 2026-07-03 — renamed inventory host homelab01 → atlas and SSH alias homelab-host → atlas (matches the server's OS hostname and the Flux clusters/atlas path); dropped now-redundant ansible_host override in hosts.yml/bootstrap.yml since the host key matches the SSH alias directly; group `homelab` unchanged

- 2026-07-03 — removed .claude/commands/ (check slash command) and docs/planning/archive/ (historical sprint write-ups); updated CLAUDE.md and LOG.md header to remove stale references

- 2026-07-03 — audit cleanup: deleted orphan `roles/workstation_setup/`, dropped dead `community.crypto` from requirements.yml, fixed stale `workstation_setup` references in architecture.md + check.md, updated flux_auth/flux_bootstrap fail_msg to point at `scripts/init-workstation.sh`, fixed false k3s defaults comment, updated secrets.sops.yml.example age path to `~/.homelab-secrets/age/`

- 2026-06-27 — consolidated workstation setup: new `workstation_setup` role generates all three keys (ansible SSH, flux-deploy SSH, SOPS age) on demand during bootstrap; renamed `bootstrap_access`→`bootstrap_user`, removed `bootstrap_key`, trimmed `flux_auth` to verify/gate only; moved all workstation files to `~/.homelab/` and retired `configure-host.sh`

- 2026-06-26  Move host/ contents to repo root — ansible.cfg, inventory/, roles/, playbooks/ now at root; Makefile drops cd host; install-deps.sh updated; CLAUDE.md/README/check command updated
- 2026-06-26  Split cluster GitOps tree into standalone homelab-cluster repo (root layout, fresh history); host repo renamed homelab-host; Flux bootstrap repointed to homelab-cluster/clusters/homelab; cluster/ removed from host repo; docs/CLAUDE.md/README/check command updated
- 2026-06-25  Add scripts/configure-host.sh — writes ~/.ssh/config.d/homelab alias (homelab-host → real address); inventories switch to ansible_host: homelab-host, rename vps01 → homelab01; backup-secrets.sh adds ~/.ssh/config.d/homelab to backup/restore; real IP no longer committed
- 2026-06-25  Remove preflight.sh; move tools/homelab-secrets.sh → scripts/backup-secrets.sh, add verbose backup/restore output; rename scripts to verb-noun pattern (install-deps, backup-secrets); drop empty tools/
- 2026-06-25  Add scripts/install-deps.sh — idempotent one-command workstation setup via apt (ansible, ansible-lint, openssh-client, sops, age) + ansible-galaxy collections; replaces manual apt block in README/CLAUDE.md; remove sshpass (not needed — bootstrap uses interactive --ask-pass)
- 2026-06-24  Make SOPS/Age mandatory for Flux bootstrap — remove flux_bootstrap_enable_sops flag, add unconditional fail-fast guard for missing Age key, drop when: gates from sops-age injection and verify tasks, tighten Cloudflare token comment and preflight warning, update docs
- 2026-06-24  Fresh-clone preflight + docs consistency — add read-only host/scripts/preflight.sh readiness report (workstation, host bootstrap, normal Ansible access, Flux bootstrap, GitOps secrets, docs/config); rewrite README first setup into staged flow; fix active docs for split k3s/flux_auth/flux_bootstrap tags; document archive as historical
- 2026-06-24  Pass 4 secret-ownership decision — Option A: keep current secret ownership; Flux deploy key stays workstation-generated (flux_auth), SOPS age key stays workstation-file (flux_bootstrap), Cloudflare token stays cluster/ SOPS manifest; inventory SOPS intentionally deferred (no per-host Ansible secret exists); .example updated (k3s→flux_auth/flux_bootstrap + deferral note); architecture.md documents ownership model + cluster/.sops.yaml placeholder reminder; inventory SOPS added to Someday
- 2026-06-24  Split Flux auth/bootstrap out of roles/k3s — k3s now installs K3s only; flux_auth owns the workstation deploy-key lifecycle (generate/display/pause/verify); flux_bootstrap owns CLI install + flux bootstrap git + sops-age Secret injection; site.yml adds flux_auth + flux_bootstrap after k3s; verify.yml splits Flux checks into flux_bootstrap; k3s_flux_* vars renamed to flux_auth_*/flux_bootstrap_*; docs/CLAUDE.md updated
- 2026-06-24  Separate bootstrap flow + split inventory var tiers — replace users role + first-run site.yml hack with dedicated playbooks/bootstrap.yml (bootstrap_key generates the local Ansible SSH key, bootstrap_access provisions ansible user); two inventories (bootstrap.yml password, hosts.yml key); all.yml trimmed to access vars only; homelab/vars.yml holds operational vars (firewall ports, k3s_version, flux_version, flux_bootstrap_*); host_vars/homelab.yml deleted (homelab is a group); requirements.yml fixed (add community.general/ufw, drop community.sops); ansible.cfg drops remote_user and privilege_escalation block; docs synced
- 2026-06-23  Kill .env + Makefile + setup.sh — non-secret config moves to inventory (host_vars/servers.yml); Flux deploy key generation moves into k3s role (delegate_to: localhost, idempotent); k8s-secrets role deleted; new flux-sops role seeds flux-system/sops-age from workstation age key; Cloudflare token moves to cluster/ as SOPS-encrypted Secret; Flux infra-configs Kustomization gains decryption block
- 2026-06-20  Split the overloaded Makefile verbs into explicit targets — `deploy`→`deploy-user`/`deploy-all`/`deploy-tag`, `check`→`check-all`/`check-tag`, `verify`→`verify-all`/`verify-tag`; the `-tag` targets and `deploy-user` prompt for the role/admin when `TAG=`/`ADMIN=` is omitted (still accept the var to skip the prompt); dropped now-unused `TAGS_ARG`/`ADMIN_ARGS` helpers and the niche first-run dry-run via `check ADMIN=`; updated README, architecture.md, setup.sh closing message
- 2026-06-20  Flatten k8s-secrets — single secret task file `cert-manager-secret.yml` folded into the role's `main.yml` (no longer empty); `site.yml` runs it as a normal role after k3s instead of a tagged `import_role`; dropped the per-secret `cert-manager-secret` tag everywhere (site/verify playbooks, setup.sh, architecture.md, CLAUDE.md role map); verify.yml now checks only the `cloudflare-api-token` it injects (removed the `apps-wildcard-tls` check — that's a cert-manager/Flux artifact, not Ansible's)
- 2026-06-20  Remove `users_admins` — dropped the additional-human-admin feature (3 tasks + `users_admins: []` default + group_vars example); the `users` role now provisions only the key-only `ansible` automation user, with the single human admin created during the Debian install; updated CLAUDE.md access model
- 2026-06-20  setup.sh env refactor — merged `example_default`/`env_value` into one `read_env` (expands a leading `$HOME`), unified the two `.env` writers into one `set_env`; `.env.example` is now the single source of truth for every default (fixes a fresh-run bug where `$HOME` was read literally); clarified the quoting rule (single-quote secrets, leave `$HOME` paths unquoted)
- 2026-06-20  Doc/config consistency pass — fixed architecture.md value table after the ANSIBLE_HOST refactor (server IP→.env, hosts.ini committed & IP-free, dropped hosts.example.ini + flux_github_path); corrected security wording (only `ansible` forced key-only, admin keeps break-glass password); removed orphan `timezone` var from all.yml (Rule 6) + its mention in CLAUDE.md/architecture.md; added k8s-secrets to the architecture role diagram + order; updated /check command to expect a committed IP-free hosts.ini
- 2026-06-20  Remove backup/restore — deleted backup.sh, restore.sh, make backup-create/backup-apply; removed backup section from README and backups/ from .gitignore
- 2026-06-20  Server IP moved to .env as ANSIBLE_HOST — hosts.ini now committed (just [servers]/homelab), ansible_host set via group_vars/servers.yml lookup; hosts.example.ini removed; setup.sh, backup.sh, restore.sh updated to drop hosts.ini handling
- 2026-06-20  SSH keys consolidated under a homelab-specific SSH directory; ansible_automation_key renamed to ansible_automation_ssh_key_file and moved from all.yml hardcode into .env/env.example as ANSIBLE_AUTOMATION_SSH_KEY_FILE; backup/restore read both key paths from .env instead of using env var overrides
- 2026-06-20  Flux bootstrap variables refactored — `FLUX_GITHUB_OWNER` + `FLUX_GITHUB_REPO` → `FLUX_BOOTSTRAP_SSH_URL` (single value, maps directly to `--url=`, works with any git host); `FLUX_DEPLOY_KEY_FILE` → `FLUX_BOOTSTRAP_SSH_KEY_FILE`; `flux_github_path` → `FLUX_BOOTSTRAP_PATH` moved from role defaults into `.env`/`.env.example` (enables per-`.env` cluster selection); setup.sh, k3s defaults/tasks, architecture.md, check command all updated
- 2026-06-20  k8s-secrets runs automatically — `make deploy` already ran it after the k3s role (namespace + secret created before Flux deploys cert-manager); fixed stale "run after Flux" comment, added k8s-secrets to README role table + role-by-role steps, updated architecture.md command to `make deploy TAGS=cert-manager-secret`
- 2026-06-20  Tighten docs — concise pass over root `CLAUDE.md`, `README.md`, `docs/architecture.md` (in place, every point kept): collapsed multi-sentence prose, trimmed redundant code-block comments, de-repeated within each file; fixed a stale "see Flux bootstrap below" → "above" in architecture.md
- 2026-06-20  backup/restore — `make backup-create` (backup.sh) saves `.env`, `inventory/hosts.ini` + the setup-generated SSH keys (Flux deploy key, `ansible` automation key) to the next numbered slot `backups/backup-<N>/`; `make backup-apply` (restore.sh) restores the highest slot (or `N=<number>`) to their original paths; each slot gets a self-describing `NOTES.txt` (what each file is + where it belongs); `backups/` gitignored so private keys/token never land in the repo
- 2026-06-20  Root `Makefile` — wraps Ansible so commands run from the repo root, auto-`cd host` + loads `.env`; targets grouped in `make help` by workstation (`setup`/`lint`/`list-tags`) vs server (`check`/`deploy`/`verify`), `TAGS=<role>` to narrow, `ADMIN=<admin>` flips deploy/check into first-run bootstrap (users-only, password auth); README getting-started + setup.sh closing message switched to `make` (first prereq: `apt-get install make`)
- 2026-06-20  Rename `infra/` → `host/` — folder name now matches the host-machine vs cluster boundary; refs updated across docs/CLAUDE.md/README/check command (archive left frozen), `cluster/` unchanged
- 2026-06-20  Drop `ADMIN_USERNAME` from `.env`/setup.sh — no role consumes it; the bootstrap admin is passed inline once as `-u <admin>`
- 2026-06-20  Drop manual `ssh` host-key step — `StrictHostKeyChecking=accept-new` in ansible.cfg auto-trusts the server on first run (still errors on changed keys); remove the one-time `ssh` from README + setup.sh
- 2026-06-20  Flip access model — install-time admin (password) bootstraps a key-only `ansible` user the `users` role provisions (workstation-generated key, locked password); `setup.sh` drops `ssh-copy-id` (+`sshpass`)
- 2026-06-20  Remove unimplemented samba + adguard roles (dirs, site.yml, docs, backlog port rows)
- 2026-06-20  Switch planning to TASKS.md + LOG.md (retire per-sprint files); remove TLS backup/restore machinery from k8s-secrets
- 2026-06-20  Headlamp no-login — chart 0.43.0 + `config.unsafeUseServiceAccountToken`
- 2026-06-20  Consolidate docs/rules/planning to repo root (single CLAUDE.md / README / docs / planning)
- 2026-06-16  cert-manager — wildcard TLS `*.apps.<domain>` via Let's Encrypt DNS-01 (Cloudflare); Headlamp over HTTPS
- 2026-06-12  Headlamp + Flux plugin — first Flux-managed app under `cluster/apps/`
- 2026-06-12  Flux `cluster/` layout — migrate flat `apps/` → `cluster/clusters/homelab` + `cluster/apps/`
- 2026-06-12  healthchecks — read-only `playbooks/verify.yml` (cockpit + k3s health)
- 2026-06-12  flux-deploy-key — token-less Flux bootstrap via repo-scoped SSH deploy key
- 2026-06-11  dotenv-secrets — workstation `.env` as the single secret source (drop ansible-vault)
- 2026-05-30  k3s — install K3s platform + bootstrap Flux CD
- 2026-05-30  cockpit — web management UI on `:9090`
- 2026-05-30  firewall — ufw default-deny, ports from `group_vars`
- 2026-05-30  security — SSH hardening, fail2ban, unattended-upgrades
- 2026-05-30  users — admin user, SSH key, passwordless sudo for `ansible`
- 2026-05-30  bootstrap — repo structure, role skeletons, docs
