---
all:
  children:
    homelab:
      hosts:
        atlas:
          ansible_user: "{{ ansible_admin_user }}"
          ansible_become: true
          ansible_ssh_private_key_file: "{{ ansible_admin_key_path }}"
