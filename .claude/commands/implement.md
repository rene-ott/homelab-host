Implement only the current `## Now` item from `docs/planning/TASKS.md`.

Before editing:

1. Read `CLAUDE.md`
2. Read `docs/planning/TASKS.md`
3. State what is in scope
4. State what is out of scope
5. State the files you intend to touch

Rules:

- Follow `CLAUDE.md` exactly.
- Make the smallest coherent change.
- Do not start anything from `## Next` or `## Someday`.
- Do not create `LOG.md`, changelogs, `architecture.md`, per-task files, migration plans, or TODO inventories.
- Do not clear `## Now`.
- Do not commit.
- Do not add AI attribution anywhere.

After editing:

1. Show the diff summary
2. List exact verification commands
3. List any risks or follow-up decisions for the human