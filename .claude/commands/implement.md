Run the **Implement** phase from the workflow contract in `CLAUDE.md`.

Before editing, state: in scope, out of scope, and the files you intend to touch.

Make the smallest coherent change for the current `## Now` item. If `Now` has a
sub-checklist, implement only the first unchecked box, then stop.

Do not clear `Now`, check off the box, or commit. After editing, show the diff
summary, list the exact verification commands the plan specified for this box, and
list any risks or follow-up decisions for the human.
