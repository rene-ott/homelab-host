Park found work without derailing the current task. This is the *only* action — do
not fix it, scope it, or leave the current box.

1. Append a single concise item to `## Someday` in `docs/planning/TASKS.md` — title +
   one-line intent, house style, no status fields.
2. If it duplicates an existing `Someday`/`Next` item, say so and add nothing.
3. State in one line that you are returning to the current `## Now` box — and do.

Do not promote it, act on it, or expand the current scope to include it.
